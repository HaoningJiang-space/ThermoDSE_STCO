#!/usr/bin/env bash
set -euo pipefail

# ThermoDSE can request visualization for debug runs. The optimizer only needs
# gcc.grid.steady, so keep this script non-fatal when plotting inputs vary.
exit 0
